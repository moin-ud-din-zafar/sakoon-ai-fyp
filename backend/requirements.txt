# Sakoon AI Backend Dependencies

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Database
mysql-connector-python>=8.2.0

# AI / ML
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
joblib>=1.3.0

# LLM & API (OpenRouter + HuggingFace)
httpx>=0.25.0
huggingface-hub>=0.23.0
transformers>=4.40.0
torch>=2.0.0
sentencepiece>=0.1.99
langdetect>=1.0.9

# TTS
edge-tts>=6.1.0

# Talking Avatar (optional — only needed if USE_XTTS=true or Wav2Lip is set up)
# Uncomment to install:
# TTS>=0.22.0          # Coqui XTTS-v2
# soundfile>=0.12.1    # WAV read/write for XTTS output
# pydub>=0.25.1        # MP3→WAV conversion (requires ffmpeg in PATH)

# Config
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Optional: imbalanced-learn for SMOTE
imbalanced-learn>=0.11.0
